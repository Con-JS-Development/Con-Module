import { LocationInUnloadedChunkError, Vector } from "@minecraft/server";
import "con-api";
world.beforeEvents.chatSend.subscribe(({message,sender})=>{
    if(message.startsWith("/")){
        
    }
});

